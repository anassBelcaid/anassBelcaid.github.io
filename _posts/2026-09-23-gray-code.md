---
layout: post
title: "Gray Code: Building a Cube One Bit at a Time"
tags: competitive leetcode bit-manipulation graph-theory
giscus_comments: true
date: 2026-09-23
featured: true
math: true
authors:
  - name: A.Belcaid
    affiliations:
      name: ENSA, Tetouan
---

Some LeetCode problems are memorable because they teach an algorithm. This one stayed with me for a different reason: it started as a confusing ordering problem and ended as a tour through geometry, recursion, and one surprisingly small bitwise formula.

This is the path I followed while solving [Gray Code — LeetCode 89](https://leetcode.com/problems/gray-code/).

## 1. Gray codes and Gray cubes

A [Gray code](https://en.wikipedia.org/wiki/Gray_code) is an ordering of binary words in which two consecutive words differ in exactly one bit. For example, `01 → 11` is a valid step because only the left bit changes; `01 → 10` is not.

There is a geometric way to see the same definition. Take the [hypercube graph](https://en.wikipedia.org/wiki/Hypercube_graph) $$Q_d$$:

- each vertex is one of the $$2^d$$ binary words of length $$d$$;
- two vertices share an edge exactly when their words differ in one bit.

A cyclic Gray code is therefore a cycle that visits every vertex of $$Q_d$$ exactly once—a Hamiltonian cycle through the Boolean cube. This was the first idea that made the problem feel visual rather than arbitrary.

### The cubes grow recursively

Every new dimension is made from **two copies** of the previous dimension. Prefix the first copy with `0`, prefix the second with `1`, and connect matching vertices.

<div class="cube-growth" aria-label="Hypercubes growing from dimension zero to dimension three">
  <svg viewBox="0 0 1040 300" role="img" aria-labelledby="cube-growth-title cube-growth-desc">
    <title id="cube-growth-title">From a point to a segment, a square, and a cube</title>
    <desc id="cube-growth-desc">The diagrams show Q zero, Q one, Q two, and Q three. Each dimension duplicates the previous graph and joins corresponding vertices.</desc>
    <defs>
      <linearGradient id="cube-edge-gradient" x1="0" x2="1">
        <stop offset="0" stop-color="#6d5dfc"/><stop offset="1" stop-color="#06b6d4"/>
      </linearGradient>
      <filter id="cube-glow" x="-50%" y="-50%" width="200%" height="200%">
        <feGaussianBlur stdDeviation="4" result="blur"/><feMerge><feMergeNode in="blur"/><feMergeNode in="SourceGraphic"/></feMerge>
      </filter>
    </defs>

    <g class="cube-stage" style="--stage: 0">
      <text class="cube-title" x="105" y="36">Q₀ · point</text>
      <circle class="cube-node" cx="105" cy="145" r="9"/>
      <text class="cube-label" x="105" y="178">ε</text>
    </g>

    <path class="cube-next" style="--stage: 1" d="M190 145h54"/><path class="cube-next-head" style="--stage: 1" d="m238 138 10 7-10 7"/>

    <g class="cube-stage" style="--stage: 1">
      <text class="cube-title" x="340" y="36">Q₁ · segment</text>
      <path class="cube-edge" d="M295 145H385"/>
      <circle class="cube-node" cx="295" cy="145" r="9"/><circle class="cube-node" cx="385" cy="145" r="9"/>
      <text class="cube-label" x="295" y="178">0</text><text class="cube-label" x="385" y="178">1</text>
    </g>

    <path class="cube-next" style="--stage: 2" d="M440 145h54"/><path class="cube-next-head" style="--stage: 2" d="m488 138 10 7-10 7"/>

    <g class="cube-stage" style="--stage: 2">
      <text class="cube-title" x="590" y="36">Q₂ · square</text>
      <path class="cube-edge" d="M535 95H645V205H535Z"/>
      <circle class="cube-node" cx="535" cy="95" r="9"/><circle class="cube-node" cx="645" cy="95" r="9"/>
      <circle class="cube-node" cx="645" cy="205" r="9"/><circle class="cube-node" cx="535" cy="205" r="9"/>
      <text class="cube-label" x="518" y="79">00</text><text class="cube-label" x="662" y="100">01</text>
      <text class="cube-label" x="662" y="211">11</text><text class="cube-label" x="518" y="234">10</text>
    </g>

    <path class="cube-next" style="--stage: 3" d="M700 145h54"/><path class="cube-next-head" style="--stage: 3" d="m748 138 10 7-10 7"/>

    <g class="cube-stage" style="--stage: 3">
      <text class="cube-title" x="885" y="36">Q₃ · cube</text>
      <g class="cube-back"><path class="cube-edge" d="M845 75H955V185H845Z"/></g>
      <path class="cube-edge" d="M805 115H915V225H805ZM805 115l40-40m70 40 40-40m-40 150 40-40m-150 40 40-40"/>
      <circle class="cube-node" cx="845" cy="75" r="7"/><circle class="cube-node" cx="955" cy="75" r="7"/>
      <circle class="cube-node" cx="955" cy="185" r="7"/><circle class="cube-node" cx="845" cy="185" r="7"/>
      <circle class="cube-node" cx="805" cy="115" r="7"/><circle class="cube-node" cx="915" cy="115" r="7"/>
      <circle class="cube-node" cx="915" cy="225" r="7"/><circle class="cube-node" cx="805" cy="225" r="7"/>
      <text class="cube-note" x="885" y="272">duplicate + connect</text>
    </g>
  </svg>
</div>

The same construction appears in the codes. If $$G_d$$ is a Gray code for dimension $$d$$, then the reflected recurrence is

$$
G_{d+1} = 0\,G_d \;\Vert\; 1\,\operatorname{reverse}(G_d),
$$

where prefixing a list means prefixing every word in that list, and $$\Vert$$ means concatenation.

<div class="gray-recurrence" aria-label="Gray code recurrence from dimension zero through dimension three">
  <div class="gray-recurrence__row"><span>G<sub>0</sub></span><code>ε</code><i>one point</i></div>
  <div class="gray-recurrence__arrow">duplicate · prefix · reflect</div>
  <div class="gray-recurrence__row"><span>G<sub>1</sub></span><code><b>0</b> &nbsp; <b>1</b></code><i>0, 1</i></div>
  <div class="gray-recurrence__arrow">duplicate · prefix · reflect</div>
  <div class="gray-recurrence__row"><span>G<sub>2</sub></span><code><b>0</b>0 &nbsp; <b>0</b>1 &nbsp; <em>1</em>1 &nbsp; <em>1</em>0</code><i>0, 1, 3, 2</i></div>
  <div class="gray-recurrence__arrow">duplicate · prefix · reflect</div>
  <div class="gray-recurrence__row"><span>G<sub>3</sub></span><code><b>0</b>00 &nbsp; <b>0</b>01 &nbsp; <b>0</b>11 &nbsp; <b>0</b>10 &nbsp; <em>1</em>10 &nbsp; <em>1</em>11 &nbsp; <em>1</em>01 &nbsp; <em>1</em>00</code><i>0, 1, 3, 2, 6, 7, 5, 4</i></div>
</div>

The blue half is the old order. The violet half is its reflection. That reversal will turn out to be the detail that makes everything work.

### What LeetCode asks for

Given `n`, return all $$2^n$$ integers from `0` through $$2^n-1$$, starting with `0`, without repetition, so that every adjacent pair differs in exactly one bit. The final and first values must differ by one bit too, so the result closes into a cycle.

<div class="gray-callout" role="note">
  <span class="gray-callout__icon" aria-hidden="true">1</span>
  <div>
    <strong>The invariant</strong>
    <p>At every step, one bit flips. Not zero bits, not two: exactly one.</p>
  </div>
</div>

### What should the result look like?

### One bit

For `n = 1`, there are only two values:

```text
Input:  n = 1
Output: [0, 1]
```

In binary, the cycle is simply `0 → 1 → 0`. Its only bit changes on both transitions.

### Two bits

For `n = 2`, one valid answer is:

```text
Input:  n = 2
Output: [0, 1, 3, 2]
```

The decimal values can hide the pattern, so let us read the same result in binary.

<div class="gray-demo" aria-label="Animated two-bit Gray code cycle">
  <div class="gray-demo__header">
    <div>
      <span class="gray-demo__eyebrow">n = 2 · 4 states</span>
      <h3>A complete one-bit cycle</h3>
    </div>
    <span class="gray-demo__legend"><i></i> flipped bit</span>
  </div>

  <div class="gray-track">
    <div class="gray-state" style="--step: 0">
      <span class="gray-state__index">0</span>
      <code><b>0</b>0</code>
      <small>decimal 0</small>
    </div>
    <span class="gray-arrow" style="--step: 1" aria-hidden="true">→</span>
    <div class="gray-state" style="--step: 1">
      <span class="gray-state__index">1</span>
      <code>0<b>1</b></code>
      <small>decimal 1</small>
    </div>
    <span class="gray-arrow" style="--step: 2" aria-hidden="true">→</span>
    <div class="gray-state" style="--step: 2">
      <span class="gray-state__index">2</span>
      <code><b>1</b>1</code>
      <small>decimal 3</small>
    </div>
    <span class="gray-arrow" style="--step: 3" aria-hidden="true">→</span>
    <div class="gray-state" style="--step: 3">
      <span class="gray-state__index">3</span>
      <code>1<b>0</b></code>
      <small>decimal 2</small>
    </div>
    <span class="gray-arrow gray-arrow--return" style="--step: 4" aria-label="and back to the start">↩</span>
  </div>

  <p class="gray-demo__caption">The highlight travels with the only bit that changes. The return edge, <code>10 → 00</code>, also flips one bit.</p>
</div>

There may be more than one correct result. For example, `[0, 2, 3, 1]` is also valid for `n = 2`. LeetCode accepts **any** sequence satisfying all five rules.

### A larger expected result

With `n = 3`, we need all eight values. One valid sequence is:

```text
[0, 1, 3, 2, 6, 7, 5, 4]
```

<div class="gray-table-wrap">
  <table class="gray-table">
    <thead>
      <tr><th>Step</th><th>Decimal</th><th>Binary</th><th>Bit flipped</th></tr>
    </thead>
    <tbody>
      <tr><td>0</td><td>0</td><td><code>000</code></td><td>start</td></tr>
      <tr><td>1</td><td>1</td><td><code>001</code></td><td>bit 0</td></tr>
      <tr><td>2</td><td>3</td><td><code>011</code></td><td>bit 1</td></tr>
      <tr><td>3</td><td>2</td><td><code>010</code></td><td>bit 0</td></tr>
      <tr><td>4</td><td>6</td><td><code>110</code></td><td>bit 2</td></tr>
      <tr><td>5</td><td>7</td><td><code>111</code></td><td>bit 0</td></tr>
      <tr><td>6</td><td>5</td><td><code>101</code></td><td>bit 1</td></tr>
      <tr><td>7</td><td>4</td><td><code>100</code></td><td>bit 0</td></tr>
      <tr class="gray-table__return"><td>↩</td><td>0</td><td><code>000</code></td><td>bit 2</td></tr>
    </tbody>
  </table>
</div>

Notice that the repeated `0` in the final row is only there to visualize the closing edge; it is **not** included a second time in the returned array.

## 2. My first idea: group by the number of ones

At first the problem looked much harder than its statement. If I ignored the binary representation, I was left trying to permute $$2^n$$ integers under a condition that felt difficult to control.

My first instinct was to group numbers by their number of set bits. A one-bit move always changes the number of ones by exactly one, so perhaps I could distribute the values layer by layer.

For `n = 3`, the layers look like this:

<div class="weight-layers" aria-label="Three-bit values grouped by Hamming weight">
  <div class="weight-layer"><span class="weight-layer__name">weight 0</span><div><code>000</code></div><strong>C(3,0) = 1</strong></div>
  <div class="weight-layer"><span class="weight-layer__name">weight 1</span><div><code>001</code><code>010</code><code>100</code></div><strong>C(3,1) = 3</strong></div>
  <div class="weight-layer"><span class="weight-layer__name">weight 2</span><div><code>011</code><code>101</code><code>110</code></div><strong>C(3,2) = 3</strong></div>
  <div class="weight-layer"><span class="weight-layer__name">weight 3</span><div><code>111</code></div><strong>C(3,3) = 1</strong></div>
</div>

There are $$\binom{n}{k}$$ values containing exactly $$k$$ ones, so the groups do not have equal sizes. Worse, knowing that the next value belongs to the neighboring layer tells me only its **weight**, not which exact bit should change. The grouping exposes the cube's bipartite structure, but it does not give me the tour.

I needed a stronger structure. That is when the recurrence came back to mind:

> If I already know how to walk through a $$d$$-cube, can I use that walk to build one through a $$(d+1)$$-cube?

### Two segments, one square

Start with the one-dimensional answer `[0, 1]`. A square is just two copies of this segment living in a two-bit world:

<div class="recurrence-story" aria-label="Animated construction of a square from a one-dimensional Gray code">
  <svg viewBox="0 0 1120 360" role="img" aria-labelledby="recurrence-story-title recurrence-story-desc">
    <title id="recurrence-story-title">Building Q two from Q one</title>
    <desc id="recurrence-story-desc">Start with the segment zero to one, duplicate it, translate the right copy by setting bit one, arrange both copies as a square, and traverse the second copy in reverse.</desc>
    <defs>
      <marker id="rec-arrow" markerWidth="8" markerHeight="8" refX="7" refY="4" orient="auto">
        <path d="M0 0 8 4 0 8Z" fill="#98a2b3"/>
      </marker>
      <marker id="cycle-arrow" markerWidth="8" markerHeight="8" refX="7" refY="4" orient="auto">
        <path d="M0 0 8 4 0 8Z" fill="#06b6d4"/>
      </marker>
    </defs>

    <g class="rec-stage rec-stage--one">
      <text class="rec-kicker" x="125" y="35">1 · START</text>
      <text class="rec-title" x="125" y="62">one segment</text>
      <path class="rec-edge" d="M70 170H180"/>
      <circle class="rec-node" cx="70" cy="170" r="18"/><circle class="rec-node" cx="180" cy="170" r="18"/>
      <text class="rec-value" x="70" y="176">0</text><text class="rec-value" x="180" y="176">1</text>
      <text class="rec-caption" x="125" y="220">G₁ = [0, 1]</text>
    </g>

    <path class="rec-flow" d="M230 170H285" marker-end="url(#rec-arrow)"/>
    <text class="rec-flow-label" x="258" y="151">duplicate</text>

    <g class="rec-stage rec-stage--two">
      <text class="rec-kicker" x="405" y="35">2 · COPY</text>
      <text class="rec-title" x="405" y="62">two identical segments</text>
      <path class="rec-edge rec-edge--zero" d="M330 130H410"/>
      <circle class="rec-node rec-node--zero" cx="330" cy="130" r="16"/><circle class="rec-node rec-node--zero" cx="410" cy="130" r="16"/>
      <text class="rec-value" x="330" y="136">0</text><text class="rec-value" x="410" y="136">1</text>
      <path class="rec-edge rec-edge--one" d="M400 215H480"/>
      <circle class="rec-node rec-node--one" cx="400" cy="215" r="16"/><circle class="rec-node rec-node--one" cx="480" cy="215" r="16"/>
      <text class="rec-value" x="400" y="221">0</text><text class="rec-value" x="480" y="221">1</text>
      <path class="rec-copy-motion" d="M370 150Q395 175 440 194" marker-end="url(#rec-arrow)"/>
      <text class="rec-caption" x="405" y="270">[0, 1] and [0, 1]</text>
    </g>

    <path class="rec-flow" d="M525 170H585" marker-end="url(#rec-arrow)"/>
    <text class="rec-flow-label" x="555" y="142">set new bit</text>
    <text class="rec-formula" x="555" y="162">x | 2¹</text>

    <g class="rec-stage rec-stage--three">
      <text class="rec-kicker" x="715" y="35">3 · TRANSLATE</text>
      <text class="rec-title" x="715" y="62">give each copy its prefix</text>
      <path class="rec-edge rec-edge--zero" d="M650 130H730"/>
      <circle class="rec-node rec-node--zero" cx="650" cy="130" r="18"/><circle class="rec-node rec-node--zero" cx="730" cy="130" r="18"/>
      <text class="rec-value" x="650" y="136">00</text><text class="rec-value" x="730" y="136">01</text>
      <path class="rec-edge rec-edge--one" d="M700 220H780"/>
      <circle class="rec-node rec-node--one" cx="700" cy="220" r="18"/><circle class="rec-node rec-node--one" cx="780" cy="220" r="18"/>
      <text class="rec-value" x="700" y="226">10</text><text class="rec-value" x="780" y="226">11</text>
      <text class="rec-prefix rec-prefix--zero" x="690" y="102">0 · [0, 1]</text>
      <text class="rec-prefix rec-prefix--one" x="740" y="264">1 · [0, 1]</text>
    </g>

    <path class="rec-flow" d="M825 170H880" marker-end="url(#rec-arrow)"/>
    <text class="rec-flow-label" x="852" y="151">connect</text>

    <g class="rec-stage rec-stage--four">
      <text class="rec-kicker" x="995" y="35">4 · REFLECT</text>
      <text class="rec-title" x="995" y="62">the square appears</text>
      <path class="rec-square" d="M930 110H1060V240H930Z"/>
      <path class="rec-cycle" d="M947 110H1043Q1060 110 1060 127V223Q1060 240 1043 240H947Q930 240 930 223V127Q930 110 947 110Z" marker-end="url(#cycle-arrow)"/>
      <circle class="rec-node rec-node--zero" cx="930" cy="110" r="18"/><circle class="rec-node rec-node--zero" cx="1060" cy="110" r="18"/>
      <circle class="rec-node rec-node--one" cx="1060" cy="240" r="18"/><circle class="rec-node rec-node--one" cx="930" cy="240" r="18"/>
      <text class="rec-value" x="930" y="116">00</text><text class="rec-value" x="1060" y="116">01</text>
      <text class="rec-value" x="1060" y="246">11</text><text class="rec-value" x="930" y="246">10</text>
      <text class="rec-caption rec-caption--strong" x="995" y="294">00 → 01 → 11 → 10</text>
      <text class="rec-caption" x="995" y="320">walk through the second copy backwards</text>
    </g>
  </svg>
</div>

The left copy receives a leading `0`. The right copy receives a leading `1`. But if I keep both copies in the same direction, the middle jump is `01 → 10`, which flips two bits. The fix is beautifully small: **walk through the second copy backwards**.

Now every kind of edge is safe:

- inside either copy, the inductive solution changes one bit;
- between the copies, only the new leading bit changes;
- the reflected order also makes the last word adjacent to the first.

This explains the entire point → segment → square → cube animation above. A $$(d+1)$$-cube is two translated $$d$$-cubes joined vertex to corresponding vertex.

### The recursive construction

In integer form, prefixing `0` changes nothing. Prefixing `1` sets bit `d`, which adds $$2^d$$. Iterating over the existing answer backwards performs the reflection:

```rust
impl Solution {
    pub fn gray_code(n: i32) -> Vec<i32> {
        let mut gray = vec![0];

        for bit in 0..n {
            let leading_bit = 1 << bit;

            for i in (0..gray.len()).rev() {
                gray.push(leading_bit | gray[i]);
            }
        }

        gray
    }
}
```

At round `bit`, the list doubles, so the total work is

$$1 + 2 + 4 + \cdots + 2^{n-1} = 2^n - 1.$$

The time complexity is therefore $$O(2^n)$$ and the returned array occupies $$O(2^n)$$ space. This is optimal in the output-sensitive sense: we cannot return $$2^n$$ values in less than $$O(2^n)$$ time.

## 3. The clever solution I found afterward

The recursive construction was satisfying because I could see why it worked. Then, while reading other solutions, I found that the whole reflected construction could be compressed into one expression:

$$
\operatorname{gray}(i) = i \oplus (i \gg 1).
$$

In words: XOR the ordinary binary index with a copy shifted one position to the right.

<div class="xor-demo" aria-label="Converting binary five to its Gray code using shift and XOR">
  <div><span>binary index</span><code>1 0 1</code><small>i = 5</small></div>
  <div><span>shift right</span><code>0 1 0</code><small>i &gt;&gt; 1</small></div>
  <div class="xor-demo__operator">⊕</div>
  <div class="xor-demo__result"><span>Gray code</span><code>1 1 1</code><small>gray(5) = 7</small></div>
</div>

Why does it work? Each Gray bit records whether two neighboring bits of `i` are different. When `i` increases, the usual binary carry may flip a whole suffix such as `0111 → 1000`; after the XOR, those coordinated flips collapse into a change at exactly one Gray bit. The formula is the bitwise shadow of the reflection we constructed geometrically.

The LeetCode solution becomes almost suspiciously short:

```rust
impl Solution {
    pub fn gray_code(n: i32) -> Vec<i32> {
        (0..(1 << n))
            .map(|i| i ^ (i >> 1))
            .collect()
    }
}
```

We still generate $$2^n$$ values, so the complexity remains $$O(2^n)$$ time and $$O(2^n)$$ output space. Apart from the returned vector, this version uses only $$O(1)$$ auxiliary space.

## What I am taking from this problem

The one-line solution is clever, but it is not the part I enjoyed most. The rewarding part was watching the object reveal itself: an awkward permutation became a walk on a graph; the graph became two smaller cubes; and the recursive reflection finally became an XOR.

That is why solving problems—and sometimes getting stuck on them—is fun. The final code may be four lines, while the path to those four lines can contain geometry, combinatorics, induction, and a new way of looking at bits.

Thank you, LeetCode, for a small problem with a surprisingly large cube hidden inside it.

<style>
  .gray-callout,
  .gray-demo,
  .cube-growth,
  .gray-recurrence,
  .weight-layers,
  .recurrence-story,
  .xor-demo {
    --gray-ink: #172033;
    --gray-muted: #667085;
    --gray-line: #dbe3ef;
    --gray-panel: #f8fafc;
    --gray-accent: #6d5dfc;
    --gray-cyan: #06b6d4;
  }

  .gray-callout {
    display: flex;
    gap: 1rem;
    align-items: center;
    margin: 1.75rem 0;
    padding: 1rem 1.15rem;
    color: var(--gray-ink);
    background: linear-gradient(135deg, #f5f3ff, #ecfeff);
    border: 1px solid #ddd6fe;
    border-radius: 14px;
  }

  .gray-callout__icon {
    display: grid;
    flex: 0 0 2.4rem;
    width: 2.4rem;
    height: 2.4rem;
    place-items: center;
    color: white;
    font: 700 1rem/1 monospace;
    background: var(--gray-accent);
    border-radius: 10px;
    box-shadow: 0 7px 18px rgba(109, 93, 252, .25);
  }

  .gray-callout p { margin: .2rem 0 0; color: var(--gray-muted); }

  .gray-demo {
    margin: 2rem 0;
    padding: clamp(1rem, 3vw, 1.6rem);
    overflow: hidden;
    color: var(--gray-ink);
    background:
      radial-gradient(circle at 90% 0%, rgba(109, 93, 252, .13), transparent 36%),
      #fff;
    border: 1px solid var(--gray-line);
    border-radius: 18px;
    box-shadow: 0 16px 45px rgba(23, 32, 51, .08);
  }

  .gray-demo__header {
    display: flex;
    gap: 1rem;
    justify-content: space-between;
    align-items: end;
    margin-bottom: 1.5rem;
  }

  .gray-demo__eyebrow {
    color: var(--gray-accent);
    font-size: .72rem;
    font-weight: 800;
    letter-spacing: .1em;
    text-transform: uppercase;
  }

  .gray-demo h3 { margin: .2rem 0 0; font-size: clamp(1.15rem, 3vw, 1.5rem); }
  .gray-demo h3,
  .gray-callout strong { color: var(--gray-ink); }

  .gray-demo__legend { color: var(--gray-muted); font-size: .75rem; white-space: nowrap; }
  .gray-demo__legend i {
    display: inline-block;
    width: .65rem;
    height: .65rem;
    margin-right: .3rem;
    background: var(--gray-cyan);
    border-radius: 3px;
  }

  .gray-track {
    display: grid;
    grid-template-columns: repeat(7, auto) 2rem;
    gap: clamp(.3rem, 1vw, .7rem);
    align-items: center;
    justify-content: center;
  }

  .gray-state {
    position: relative;
    min-width: 5.4rem;
    padding: .9rem .65rem .7rem;
    text-align: center;
    background: var(--gray-panel);
    border: 1px solid var(--gray-line);
    border-radius: 13px;
    animation: gray-state-pulse 6s ease-in-out infinite;
    animation-delay: calc(var(--step) * 1.2s);
  }

  .gray-state__index {
    position: absolute;
    top: -.55rem;
    left: 50%;
    display: grid;
    width: 1.1rem;
    height: 1.1rem;
    place-items: center;
    color: white;
    font: 700 .62rem/1 sans-serif;
    background: var(--gray-accent);
    border-radius: 50%;
    transform: translateX(-50%);
  }

  .gray-state code { color: var(--gray-ink); font-size: 1.45rem; letter-spacing: .12em; }
  .gray-state code b {
    color: var(--gray-cyan);
    text-shadow: 0 0 14px rgba(6, 182, 212, .4);
  }
  .gray-state small { display: block; margin-top: .35rem; color: var(--gray-muted); font-size: .66rem; }

  .gray-arrow {
    color: #a6b0c2;
    font-size: 1.3rem;
    animation: gray-arrow-pulse 6s ease-in-out infinite;
    animation-delay: calc(var(--step) * 1.2s - .35s);
  }
  .gray-arrow--return { color: var(--gray-accent); }

  .gray-demo__caption {
    margin: 1.35rem 0 0;
    color: var(--gray-muted);
    font-size: .82rem;
    text-align: center;
  }

  .gray-table-wrap { margin: 1.5rem 0; overflow-x: auto; border-radius: 14px; }
  .gray-table { width: 100%; margin: 0; border-collapse: separate; border-spacing: 0; }
  .gray-table th {
    padding: .7rem 1rem;
    color: #475467;
    font-size: .72rem;
    letter-spacing: .06em;
    text-transform: uppercase;
    background: #f1f5f9;
  }
  .gray-table td { padding: .65rem 1rem; border-bottom: 1px solid #e8edf4; }
  .gray-table tbody tr:hover { background: rgba(109, 93, 252, .05); }
  .gray-table__return td { color: #6d5dfc; font-weight: 700; border-bottom: 0; }

  @keyframes gray-state-pulse {
    0%, 16%, 100% { border-color: var(--gray-line); box-shadow: none; transform: translateY(0); }
    5%, 11% { border-color: var(--gray-cyan); box-shadow: 0 8px 24px rgba(6, 182, 212, .18); transform: translateY(-4px); }
  }

  @keyframes gray-arrow-pulse {
    0%, 16%, 100% { color: #a6b0c2; transform: scale(1); }
    5%, 11% { color: var(--gray-cyan); transform: scale(1.25); }
  }

  @media (max-width: 760px) {
    .gray-demo__header { align-items: start; }
    .gray-track { grid-template-columns: 1fr 1.5rem 1fr; }
    .gray-track > :nth-child(4) { display: none; }
    .gray-track > :nth-child(5) { grid-column: 3; }
    .gray-track > :nth-child(6) { grid-column: 2; grid-row: 2; transform: rotate(180deg); }
    .gray-track > :nth-child(7) { grid-column: 1; grid-row: 2; }
    .gray-track > :nth-child(8) { grid-column: 1 / -1; text-align: center; transform: rotate(90deg); }
  }

  @media (prefers-reduced-motion: reduce) {
    .gray-state, .gray-arrow { animation: none; }
  }

  .cube-growth {
    margin: 2rem 0;
    padding: .75rem;
    overflow-x: auto;
    color: var(--gray-ink);
    background: radial-gradient(circle at 50% 110%, rgba(6, 182, 212, .13), transparent 42%), #fff;
    border: 1px solid var(--gray-line);
    border-radius: 18px;
    box-shadow: 0 16px 45px rgba(23, 32, 51, .08);
  }

  .cube-growth svg { display: block; min-width: 720px; width: 100%; height: auto; }
  .cube-title { fill: var(--gray-ink); font: 700 17px/1 sans-serif; text-anchor: middle; }
  .cube-label { fill: var(--gray-muted); font: 700 14px/1 monospace; text-anchor: middle; }
  .cube-note { fill: var(--gray-muted); font: 600 13px/1 sans-serif; text-anchor: middle; }
  .cube-edge {
    fill: none;
    stroke: url(#cube-edge-gradient);
    stroke-width: 3;
    stroke-linecap: round;
    stroke-linejoin: round;
    stroke-dasharray: none;
    stroke-dashoffset: 0;
    animation: cube-edge-pulse 8s ease-in-out infinite;
    animation-delay: calc(var(--stage, 0) * 1.25s);
  }
  .cube-back { opacity: .42; }
  .cube-node {
    fill: #fff;
    stroke: #6d5dfc;
    stroke-width: 4;
    filter: url(#cube-glow);
    animation: cube-node-in 8s ease-in-out infinite;
    animation-delay: calc(var(--stage, 0) * 1.25s);
  }
  .cube-next, .cube-next-head {
    fill: none;
    stroke: #a9b3c4;
    stroke-width: 2;
    stroke-linecap: round;
    stroke-linejoin: round;
    animation: cube-arrow 8s ease-in-out infinite;
    animation-delay: calc(var(--stage) * 1.25s);
  }

  .gray-recurrence {
    display: grid;
    gap: .45rem;
    margin: 1.6rem 0 2rem;
    padding: 1rem;
    color: var(--gray-ink);
    background: #fff;
    border: 1px solid var(--gray-line);
    border-radius: 16px;
  }
  .gray-recurrence__row {
    display: grid;
    grid-template-columns: 3rem minmax(0, 1fr) auto;
    gap: 1rem;
    align-items: center;
    padding: .7rem .85rem;
    background: var(--gray-panel);
    border-radius: 10px;
  }
  .gray-recurrence__row > span { color: var(--gray-accent); font-weight: 800; }
  .gray-recurrence__row code { overflow-x: auto; color: var(--gray-ink); white-space: nowrap; }
  .gray-recurrence__row code b { color: #0891b2; font-style: normal; }
  .gray-recurrence__row code em { color: #7c3aed; font-style: normal; font-weight: 800; }
  .gray-recurrence__row i { color: var(--gray-muted); font-size: .76rem; font-style: normal; white-space: nowrap; }
  .gray-recurrence__arrow {
    padding-left: 4rem;
    color: #98a2b3;
    font-size: .68rem;
    font-weight: 700;
    letter-spacing: .08em;
    text-transform: uppercase;
  }

  .weight-layers {
    display: grid;
    gap: .55rem;
    margin: 1.6rem 0;
    padding: 1rem;
    color: var(--gray-ink);
    background: linear-gradient(145deg, #f8fafc, #f5f3ff);
    border: 1px solid var(--gray-line);
    border-radius: 16px;
  }
  .weight-layer {
    display: grid;
    grid-template-columns: 5.5rem 1fr 6.5rem;
    gap: .75rem;
    align-items: center;
  }
  .weight-layer__name { color: var(--gray-muted); font-size: .75rem; font-weight: 700; text-transform: uppercase; }
  .weight-layer > div { display: flex; gap: .45rem; justify-content: center; }
  .weight-layer code {
    padding: .38rem .55rem;
    color: var(--gray-ink);
    background: #fff;
    border: 1px solid #d9e1ec;
    border-radius: 7px;
    box-shadow: 0 3px 8px rgba(23, 32, 51, .05);
  }
  .weight-layer strong { color: var(--gray-accent); font: 700 .78rem/1 monospace; text-align: right; }

  .recurrence-story {
    margin: 1.8rem 0;
    padding: .75rem;
    overflow-x: auto;
    color: var(--gray-ink);
    background:
      radial-gradient(circle at 100% 100%, rgba(109, 93, 252, .12), transparent 34%),
      #fff;
    border: 1px solid var(--gray-line);
    border-radius: 18px;
    box-shadow: 0 16px 45px rgba(23, 32, 51, .08);
  }
  .recurrence-story svg { display: block; min-width: 900px; width: 100%; height: auto; }
  .rec-kicker { fill: #6d5dfc; font: 800 11px/1 sans-serif; letter-spacing: 1.4px; text-anchor: middle; }
  .rec-title { fill: #172033; font: 700 15px/1 sans-serif; text-anchor: middle; }
  .rec-edge, .rec-square {
    fill: none;
    stroke: #64748b;
    stroke-width: 4;
    stroke-linecap: round;
    stroke-linejoin: round;
  }
  .rec-edge--zero { stroke: #06b6d4; }
  .rec-edge--one { stroke: #7c3aed; }
  .rec-node { fill: #fff; stroke: #64748b; stroke-width: 4; }
  .rec-node--zero { stroke: #06b6d4; }
  .rec-node--one { stroke: #7c3aed; }
  .rec-value { fill: #172033; font: 800 12px/1 monospace; text-anchor: middle; }
  .rec-caption { fill: #667085; font: 600 12px/1 sans-serif; text-anchor: middle; }
  .rec-caption--strong { fill: #6d5dfc; font: 800 14px/1 monospace; }
  .rec-flow { fill: none; stroke: #98a2b3; stroke-width: 2; stroke-dasharray: 5 5; }
  .rec-flow-label { fill: #667085; font: 700 9px/1 sans-serif; letter-spacing: .7px; text-anchor: middle; text-transform: uppercase; }
  .rec-formula { fill: #6d5dfc; font: 800 13px/1 monospace; text-anchor: middle; }
  .rec-copy-motion { fill: none; stroke: #98a2b3; stroke-width: 1.5; stroke-dasharray: 4 4; }
  .rec-prefix { font: 800 11px/1 monospace; text-anchor: middle; }
  .rec-prefix--zero { fill: #0891b2; }
  .rec-prefix--one { fill: #7c3aed; }
  .rec-cycle {
    fill: none;
    stroke: #06b6d4;
    stroke-width: 3;
    stroke-linecap: round;
    stroke-dasharray: 500;
    animation: rec-cycle-run 5s ease-in-out infinite;
  }
  .rec-stage {
    animation: rec-stage-focus 8s ease-in-out infinite;
    transform-box: fill-box;
    transform-origin: center;
  }
  .rec-stage--one { animation-delay: 0s; }
  .rec-stage--two { animation-delay: 1.4s; }
  .rec-stage--three { animation-delay: 2.8s; }
  .rec-stage--four { animation-delay: 4.2s; }

  .reflection-demo {
    display: grid;
    grid-template-columns: 1fr auto 1fr;
    gap: .8rem;
    align-items: center;
    margin: 1.7rem 0;
    padding: 1.25rem;
    color: var(--gray-ink);
    background: #fff;
    border: 1px solid var(--gray-line);
    border-radius: 16px;
    box-shadow: 0 12px 35px rgba(23, 32, 51, .07);
  }
  .reflection-copy {
    display: grid;
    grid-template-columns: 1fr auto 1fr;
    gap: .45rem;
    align-items: center;
    padding: 1rem;
    text-align: center;
    background: var(--gray-panel);
    border: 1px solid var(--gray-line);
    border-radius: 12px;
  }
  .reflection-copy > span { grid-column: 1 / -1; margin-bottom: .4rem; color: var(--gray-muted); font-size: .72rem; font-weight: 700; text-transform: uppercase; }
  .reflection-copy code { color: var(--gray-ink); font-size: 1.25rem; }
  .reflection-copy--zero code b { color: #0891b2; }
  .reflection-copy--one code b { color: #7c3aed; }
  .reflection-copy i { color: #98a2b3; font-style: normal; }
  .reflection-bridge { color: var(--gray-accent); font-size: .7rem; font-weight: 800; letter-spacing: .08em; text-transform: uppercase; }
  .reflection-result {
    grid-column: 1 / -1;
    padding-top: .8rem;
    text-align: center;
    border-top: 1px dashed var(--gray-line);
  }
  .reflection-result code { color: var(--gray-accent); font-size: 1.05rem; font-weight: 800; }

  .xor-demo {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: .55rem;
    position: relative;
    margin: 1.8rem auto;
    padding: 1rem;
    max-width: 34rem;
    color: var(--gray-ink);
    background: #fff;
    border: 1px solid var(--gray-line);
    border-radius: 16px;
    box-shadow: 0 14px 40px rgba(23, 32, 51, .08);
  }
  .xor-demo > div:not(.xor-demo__operator) {
    display: grid;
    gap: .2rem;
    padding: .8rem;
    text-align: center;
    background: var(--gray-panel);
    border-radius: 10px;
  }
  .xor-demo span { color: var(--gray-muted); font-size: .68rem; font-weight: 700; letter-spacing: .06em; text-transform: uppercase; }
  .xor-demo code { color: var(--gray-ink); font-size: 1.3rem; font-weight: 800; letter-spacing: .16em; }
  .xor-demo small { color: var(--gray-muted); }
  .xor-demo__operator { position: absolute; top: 50%; left: 50%; z-index: 2; display: grid; width: 2rem; height: 2rem; place-items: center; color: #fff; background: var(--gray-accent); border: 4px solid #fff; border-radius: 50%; transform: translate(-50%, -50%); }
  .xor-demo__result { grid-column: 1 / -1; background: linear-gradient(135deg, #eef2ff, #ecfeff) !important; }
  .xor-demo__result code { color: var(--gray-accent); }

  @keyframes cube-edge-pulse {
    0%, 100% { opacity: .45; }
    18%, 72% { opacity: 1; }
  }
  @keyframes cube-node-in {
    0%, 8% { opacity: .2; transform: scale(.6); transform-box: fill-box; transform-origin: center; }
    18%, 82% { opacity: 1; transform: scale(1); }
    100% { opacity: .45; }
  }
  @keyframes cube-arrow {
    0%, 12% { opacity: .15; }
    18%, 28% { opacity: 1; stroke: #06b6d4; }
    40%, 100% { opacity: .35; }
  }
  @keyframes rec-stage-focus {
    0%, 100% { opacity: .68; }
    8%, 24% { opacity: 1; transform: translateY(-3px); }
    34%, 90% { opacity: .82; transform: translateY(0); }
  }
  @keyframes rec-cycle-run {
    0%, 12% { stroke-dashoffset: 500; opacity: .25; }
    48%, 82% { stroke-dashoffset: 0; opacity: 1; }
    100% { stroke-dashoffset: -500; opacity: .35; }
  }

  @media (max-width: 680px) {
    .gray-recurrence__row { grid-template-columns: 2.5rem 1fr; gap: .5rem; }
    .gray-recurrence__row i { display: none; }
    .gray-recurrence__arrow { padding-left: 3.25rem; }
    .weight-layer { grid-template-columns: 1fr; text-align: center; }
    .weight-layer strong { text-align: center; }
    .reflection-demo { grid-template-columns: 1fr; }
    .reflection-bridge { text-align: center; }
    .reflection-result { grid-column: 1; }
  }

  @media (prefers-reduced-motion: reduce) {
    .cube-edge, .cube-node, .cube-next, .cube-next-head, .rec-stage, .rec-cycle { animation: none; stroke-dashoffset: 0; opacity: 1; }
  }
</style>
